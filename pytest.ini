[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Unit tests (no server or DB required)
    integration: Integration tests (mocked DB, no server required)
    system: System/E2E tests (requires running Flask server on localhost:5000)
